# Single-package repository; this file carries pnpm settings only.
# pnpm 11 refuses to install while dependency build scripts are unreviewed,
# so the review is recorded here: esbuild may build, the rest may not.
allowBuilds:
  "@google/genai": false
  esbuild: true
  protobufjs: false
